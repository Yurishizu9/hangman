'''
In third_party.py, import pandas
You might see an error message saying that pandas is not installed. If you do, run pip install pandas in the terminal. If you don't see an error message, skip this step.
Packages are usually imported with an alias. Import pandas as pd and print the version of pandas that you have installed. You can find the version of pandas by running pd.__version__ in the terminal.
'''
import pandas as pd

print(pd.__version__)